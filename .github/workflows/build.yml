# 9.1 build→ECR push (OIDC·SHA 태그) + manifests 이미지 태그 자동 반영 (안건 A / Option A1)
name: build-and-push

on:
  push:
    # dev: 배포 흐름의 시작점(여기 머지되면 manifests 로 PR 이 나간다).
    # main: 이미지만 빌드해 둔다(아래 update-manifests 는 dev 에서만 돈다).
    branches: [main, dev]
    # 문서·프론트만 고친 커밋까지 빌드하면 SHA 태그가 바뀌어 파드가 불필요하게 재시작된다.
    paths:
      - 'backend/**'
      - 'common/**'
      - 'ml/**'
      - 'predict/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:

# 연속 머지 시 뒤엣것만 살린다. 고정 봇 브랜치를 동시에 건드리는 경합을 막고,
# 항상 최신 SHA 가 배포되게 한다.
concurrency:
  group: build-and-push-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write   # OIDC — 장기 AWS 키 없이 AssumeRole
  contents: read

env:
  AWS_REGION: ap-northeast-2

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [call-api, worker, predict, simulator, weather-cron]
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.ECR_PUSH_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 07-13 네이밍 규약에 따른 변수명 및 코드 수정 중 A-2. ECR 태그 (규약 §8: 태그는 커밋 SHA만, latest 금지)
      - name: Build and push (SHA 태그)
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
          IMAGE: hailcast-dev-${{ matrix.service }}
        run: |
          # build context = 레포 루트 (common/·ml/ 공유 모듈 COPY)
          docker build -f backend/${{ matrix.service }}/Dockerfile \
            -t "$REGISTRY/$IMAGE:${GITHUB_SHA::7}" .
          docker push "$REGISTRY/$IMAGE:${GITHUB_SHA::7}"

  # ── manifests 레포에 새 SHA 태그 반영 (안건 A) ──
  # matrix 안이 아니라 별도 job 이다. matrix 5개가 동시에 같은 레포로 push 하면 충돌한다.
  # needs: build 로 5개 빌드가 모두 끝난 뒤 '한 번만' 순차 실행된다.
  update-manifests:
    needs: build
    runs-on: ubuntu-latest
    # 배포 경로는 app dev → manifests dev 하나뿐이다. main push 는 이미지만 만들고 끝낸다.
    if: github.ref == 'refs/heads/dev'
    env:
      MANIFESTS_REPO: ThisPod-ThatPod/project3-hailcast-manifests
      # ArgoCD 가 바라보는 브랜치와 반드시 같아야 한다(predict·worker·app-of-apps 전부 dev).
      # 다르면 태그가 갱신돼도 배포되지 않는다.
      BASE_BRANCH: dev
      # 고정 이름이다. 매번 새 브랜치를 만들면 PR 이 무한 누적된다.
      BOT_BRANCH: bot/image-tag-update
    steps:
      - name: Checkout manifests
        uses: actions/checkout@v4
        with:
          repository: ${{ env.MANIFESTS_REPO }}
          ref: ${{ env.BASE_BRANCH }}
          # 크로스 레포라 기본 GITHUB_TOKEN 으로는 접근할 수 없다.
          token: ${{ secrets.MANIFESTS_PAT }}
          # 얕은 클론에서 force push 하면 'shallow update not allowed' 로 막힐 수 있다.
          fetch-depth: 0

      - name: Update image tags
        id: sed
        run: |
          set -euo pipefail
          # build 잡이 push 에 쓴 태그와 반드시 같은 값이어야 한다.
          # 한 글자라도 다르면 ECR 에 없는 태그를 가리켜 ImagePullBackOff 가 난다.
          TAG="${GITHUB_SHA::7}"

          # 매니페스트가 실제로 존재하는 2개만 대상이다.
          # call-api·weather-cron 은 .gitkeep 뿐인 빈 디렉터리, simulator 는 디렉터리 자체가 없다.
          # 나중에 매니페스트가 생기면 이 목록에 직접 추가해야 한다(빠뜨리면 그 서비스만 옛 이미지로 남는다).
          for svc in predict worker; do
            f="apps/${svc}/deployment.yaml"
            [ -f "$f" ] || { echo "::error::${f} 가 없다. 경로 규약이 바뀌었는지 확인한다."; exit 1; }

            # 패턴을 <GITHUB_SHA> 리터럴로 잡으면 1회차에 플레이스홀더가 사라져 2회차부터
            # 아무것도 매칭되지 않고 '조용히' 실패한다. 이미지 라인을 통째로 잡아 태그만 교체한다.
            #   \1 역참조 → 레지스트리 주소(계정ID)는 원본 유지. CI 가 계정ID 를 알 필요가 없다.
            #   [^[:space:]]* → 공백 앞에서 멈추므로 뒤따르는 설명 주석이 보존된다.
            sed -i -E \
              "s|^([[:space:]]*image:[[:space:]]*[^[:space:]]*hailcast-dev-${svc}):[^[:space:]]*|\1:${TAG}|" \
              "$f"

            # 치환 실패를 그냥 넘기면 옛 이미지가 계속 배포된다. 반드시 job 을 실패시킨다.
            grep -q "hailcast-dev-${svc}:${TAG}" "$f" \
              || { echo "::error file=${f}::이미지 태그 치환 실패. 이미지 라인 형식이 바뀌었는지 확인한다."; exit 1; }
          done

          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Commit & push to bot branch
        id: commit
        env:
          TAG: ${{ steps.sed.outputs.tag }}
        run: |
          set -euo pipefail

          # 이미 최신이면 커밋할 게 없다. 방치하면 git commit 이 실패해 job 이 빨개진다.
          if git diff --quiet; then
            echo "변경 없음 — 이미 ${TAG} 로 최신이다. 스킵한다."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name  "hailcast-ci[bot]"
          git config user.email "hailcast-ci[bot]@users.noreply.github.com"

          # base 에서 새로 파생시킨다 → PR diff 가 항상 '커밋 1개'로 깔끔하게 유지된다.
          git checkout -B "$BOT_BRANCH"
          git add apps/predict/deployment.yaml apps/worker/deployment.yaml

          # [skip ci]: 지금 manifests 에는 push 트리거 워크플로가 없어 당장은 무해하다.
          # 다만 PAT 로 push 하면 GitHub 의 기본 루프 방지가 걸리지 않으므로 미리 넣어 둔다.
          git commit \
            -m "chore(deploy): predict·worker 이미지 태그 ${TAG} 로 갱신 [skip ci]" \
            -m "app 레포 커밋: ${GITHUB_SHA}" \
            -m "워크플로: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

          # 봇 전용 브랜치라 강제 갱신이 의도된 동작이다(항상 base + 커밋 1개).
          git push --force origin "$BOT_BRANCH"
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Open or update PR
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.MANIFESTS_PAT }}
          TAG: ${{ steps.sed.outputs.tag }}
        run: |
          set -euo pipefail

          # 열린 PR 이 이미 있으면 위 force push 로 내용이 갱신되었으므로 새로 만들지 않는다.
          existing="$(gh pr list --repo "$MANIFESTS_REPO" --head "$BOT_BRANCH" \
                        --state open --json number --jq '.[].number')"
          if [ -n "$existing" ]; then
            echo "기존 PR #${existing} 가 ${TAG} 로 갱신되었다."
            exit 0
          fi

          cat > "$RUNNER_TEMP/pr-body.md" <<EOF
          app 레포 \`${GITHUB_SHA}\` 빌드 결과를 반영한다.

          - predict / worker 이미지 태그 → \`${TAG}\`
          - 머지하면 ArgoCD 가 \`${BASE_BRANCH}\` 를 감지해 자동 롤아웃한다.

          워크플로: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}
          EOF

          gh pr create \
            --repo "$MANIFESTS_REPO" \
            --base "$BASE_BRANCH" \
            --head "$BOT_BRANCH" \
            --title "chore(deploy): 이미지 태그 갱신 (${TAG})" \
            --body-file "$RUNNER_TEMP/pr-body.md"
